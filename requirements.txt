# Root requirements.txt - for development and testing only
# Worker dependencies (ultralytics, opencv) are in worker/requirements.txt
# API dependencies are in api/requirements.txt
pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
